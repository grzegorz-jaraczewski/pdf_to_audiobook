from django.apps import AppConfig


class JobsConfig(AppConfig):
    """
    Configuration class for the 'jobs' Django application.

    Defines the application name used by Django for app registration.
    """
    name = 'jobs'
